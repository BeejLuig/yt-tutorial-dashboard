{
  "name": "yt-tutorial-dashboard",
  "engines": {
    "node": "6.9.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && npm run build",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run deploy"
  }
}
